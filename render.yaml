services:
  - type: web
    name: priorart-copilot
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false
